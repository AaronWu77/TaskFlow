# TaskFlow 工作进度与剩余发布计划

> 最后更新：2026-09-15
>
> 当前发布代码基线：`e1257343703309fa83432fcb95cadd6183f74a6b`
>
> 生产代码基线：`47.95.226.89` / `e1257343703309fa83432fcb95cadd6183f74a6b`（2026-09-15 已发布）
>
> 维护规则：保留一份精简的已完成证据；详细步骤只保留未完成事项。勾选项必须有真实环境证据，不能用“代码存在”代替“生产验收通过”。

## 当前完成进度

### 已完成：本地代码实现与审查

- [x] 修复任务添加时“先填写名称、再修改优先级”无法提交的问题；表单提交从同一份渲染快照原子读取标题和优先级。
- [x] 修复排序面板拖动触点偏移，并恢复跨槽位去重的选择/变化/结束触觉反馈。
- [x] 收敛退出、刷新令牌、同账号重登、账号切换和旧请求回写竞态；网络、超时、协议不兼容、限流和真实离线分开展示。
- [x] 建立事务型本地任务仓库、双槽校验快照、待同步队列恢复和账号隔离。
- [x] 后端任务写入采用版本 CAS 和用户级锁；排序、同步游标、增量统计及维护清理具备并发保护。
- [x] 重复任务使用权威 `TaskSeries`、系列版本和“仅本次 / 本次及以后 / 整个系列”；计划移除与用户删除可区分，历史完成记录不会被未来计划修改复活或覆盖。
- [x] 新增账户恢复、密码重置/修改、会话管理、近期重新认证和 14 天可恢复删除。
- [x] 完成数据库约束、typed 日期影子列、设备水位、增量统计、会话绑定和系列版本等 10 个 additive migration；当前共 25 个迁移。
- [x] 完成备份、校验、隔离恢复演练、监控、告警、不可变镜像发布与双镜像回滚脚本，并加入 CI。
- [x] 修复后端 Docker 构建上下文过大：实测上下文约由 201 MB 降到 12.43 kB；补齐 ARM64 Alpine Prisma/OpenSSL 运行目标。
- [x] 移除 iOS 启动期 Google Fonts 阻塞请求，字体改为 App 内置；同一模拟器 `firstVisualLayout` 实测由 11.252 秒降到 0.159 秒，并禁止 Capacitor 注册多余 Service Worker。
- [x] 修复生产镜像构建卡住：API 从两次联网安装依赖收敛为一次安装和本地裁剪，生产迁移所需 Prisma CLI 改为运行依赖，并为 npm/Prisma 二进制提供可配置镜像源。
- [x] 生产 API/Web 已使用不可变标签 `e125734`，10 个新增迁移已无损应用；公网 readiness 和首页、三个容器健康检查均通过。

### 已完成：本地自动化与模拟器证据

- [x] `npm run check`：typecheck、前后端生产构建和 84/84 项测试通过。
- [x] `npm run test:integration`：空库顺序应用 25 个迁移，Prisma schema 无漂移，数据库约束、并发同步、幂等重放与 10,000 任务 Bootstrap 通过。
- [x] `npm run test:ops-integration`：真实 `pg_dump`、SHA 校验、空隔离库恢复、迁移、API readiness 和登录态 Bootstrap 全链路通过。
- [x] `npm run test:e2e`：移动端“标题后改优先级”提交并刷新保留通过。
- [x] 前端和后端生产依赖审计均为 0 个已知漏洞；Workflow YAML、Compose 配置、Plist/Privacy Manifest 和 `git diff --check` 通过。
- [x] Capacitor 同步和 Xcode 26.6 / iOS 26.5 Simulator Debug 构建通过；最新包已安装并启动，实际首屏正常。

### 尚未交付

- [ ] OSS 异地备份、隔离恢复演练、告警和 systemd timers 尚未部署；当前只有发布前本机备份，尚不能承诺 RPO/RTO。
- [ ] 新版生产发布仍需完成观察窗口，以及 iPhone 真机和多端同步矩阵。

## 接下来严格按此顺序执行

### P-1：冻结并交付当前代码（已完成）

1. 已核对提交范围，不包含 `.env`、密钥、备份、测试截图或临时产物；Gitleaks 远端扫描通过。
2. 主功能提交为 `c857525029cbcddf0731d5510fca2daa93ea3d7b`；经过 CI 路径修正和跨平台测试隔离后，发布候选为 `2ccde7b8d612772267614a1e4ffcba51e9187461`。
3. 发布候选已推送到 `origin/main`；GitHub Actions CI #6（run `34931404222`）的 build、unit、PostgreSQL integration、ops integration、E2E、依赖审计、密钥扫描和 iOS Simulator 全部通过。
4. 最后迁移为 `20260914100000_series_schedule_exclusions`；本地与 CI 均从空 PostgreSQL 顺序应用全部 25 个迁移且 Prisma schema 无漂移。

完成标准：

- [x] 本地工作区干净，远端能看到目标 SHA。
- [x] 目标 SHA 的 GitHub CI 全部通过。
- [x] 发布记录已写入目标 SHA 和最后迁移 `20260914100000_series_schedule_exclusions`。

## 执行顺序

顺序不可交换：

1. P-1 冻结、提交、推送并通过 CI（已完成）。
2. P1 首次生产发布（已按用户明确的数据可丢弃例外完成；账户必须保留）。
3. P0 生产异地备份、恢复和告警验收。
4. P2 iPhone 真机和多端矩阵验收。
5. P3 兼容观察期后的数据库收缩。
6. P4 不改变功能和视觉的结构清理。

任何阶段出现数据不一致、无法恢复、认证串号或未解释的离线提示，立即停止后续阶段并回滚到上一稳定镜像。不得把“构建成功”视为生产验收成功。

## P0：生产异地备份、恢复和告警验收（下一步，部分完成）

### 生产现状（2026-09-15 发布后检查）

- `/opt/TaskFlow` 已更新到 `e1257343703309fa83432fcb95cadd6183f74a6b`。
- PostgreSQL、API 和 Nginx 容器均为 healthy；API/Web 使用不可变标签 `taskflow-api:e125734`、`taskflow-web:e125734`。
- `/etc/taskflow/ops.env` 不存在，TaskFlow systemd timers 数量为 0。
- 已生成并校验 `/opt/taskflow-backups/preupdate-20260915-5a53497.dump` 及 SHA256；`pg_restore --list` 通过，但尚未上传 OSS 或完成异地下载恢复。

结论：本地发布前备份已完成，异地容灾闭环尚未开始。本次发布依据用户明确授权“任务数据可丢弃、账户必须保留”执行；该例外不等同于日常备份策略达标，后续发布前仍应完成 P0。

### 目标

- RPO 不超过 24 小时。
- RTO 不超过 2 小时。
- 生产发布前至少存在一份已经从 OSS 下载并成功恢复的备份。

### 实施步骤

1. 在阿里云创建或确认专用私有 OSS Bucket：
   - 禁止公共读写。
   - 启用版本控制。
   - 使用 SSE-KMS；暂时不能使用 KMS 时使用 SSE-OSS/AES256。
   - 生命周期保留 7 份每日、4 份每周、12 份每月备份。
   - 为服务器 RAM 角色或独立 RAM 用户只授予目标备份前缀的上传和元数据读取权限。
2. 在服务器创建 `/etc/taskflow/ops.env`，权限为 `root:root 0600`；填写 OSS 地址、`ossutil` 配置、告警 Webhook 和指标令牌。令牌不得提交到 Git。
3. 安装 `ops/systemd/` 中的 service/timer，先只手工启动一次 `taskflow-backup.service`，暂不启用定时器。
4. 核对本地 `.dump/.sha256/.meta` 和 OSS 三个对象：对象大小匹配、服务端加密字段匹配、归档能被 `pg_restore --list` 读取。
5. 从 OSS 下载这一份备份到隔离目录，使用 `ops/restore/run-restore-drill.sh` 恢复到临时 PostgreSQL；核对迁移、用户数、任务数、最大同步序号、孤立记录和重复 operationId。
6. 人工提供一次错误 OSS 目标或撤销临时写权限，确认备份失败返回非零、上一份成功备份未被删除、Webhook/系统告警能到达，然后恢复权限。
7. 验收通过后启用 backup、verify、restore-drill、monitor 四组 timer，并连续观察 3 次计划执行。

### 回滚

- 任一步失败只停用相应 timer，不删除已有备份和 OSS 对象。
- 恢复脚本只允许空的隔离数据库，禁止把 `/opt/TaskFlow` 作为目标。
- 未完成一次 OSS 下载恢复前，不把备份体系标记为生产验收通过。

### 完成标准

- [ ] 连续 3 次生产定时备份成功，本地与 OSS 元数据匹配。
- [ ] 至少 1 次从 OSS 下载后完整恢复，报告中的业务计数一致。
- [ ] 上传失败演练触发告警，上一份成功备份仍可恢复。
- [ ] API、数据库、磁盘、证书、备份年龄和恢复演练均有可到达的告警。

## P1：不可变镜像发布与观察（发布完成，观察待完成）

### 发布前检查

1. 在 GitHub Environment `production` 配置审批规则和以下 Secrets：
   - `PROD_HOST`
   - `PROD_USER`
   - `PROD_SSH_KEY`
   - `PROD_KNOWN_HOSTS`（由受信任网络预先取得并人工核对指纹，禁止发布时动态 `ssh-keyscan`）
   - `TASKFLOW_ALERT_WEBHOOK_URL`
2. 确认 `/opt/TaskFlow` 的 Git remote、当前提交、Compose 项目、数据库卷和当前 API/Web 镜像；记录两个旧镜像的不可变标签。
3. 确认 P0 的最近备份、OSS 上传和恢复报告均成功。
4. 在发布提交上重新执行 CI、PostgreSQL 集成、移动端 E2E、依赖审计和 iOS Simulator 构建。

### 发布方案

1. 通过 `.github/workflows/deploy-production.yml` 手工触发，不在服务器现场构建源码。
2. CI 以提交 SHA 分别构建 API 与 Web 镜像并推送到 GHCR，不使用 `latest`。
3. 服务器部署脚本先触发发布前备份，再拉取镜像并运行 additive migration。
4. 先启动 API，等待 readiness；通过后再启动 Nginx，并从容器内及公网各验证一次健康接口。
5. 发布后至少观察 30 分钟：登录/刷新、Bootstrap、Push/Pull、5xx、p95/p99、冲突率、容器重启、数据库连接和磁盘。

### 回滚条件和方法

以下任一情况立即回滚：readiness 失败、登录或刷新异常、5xx 明显上升、同步拒绝/冲突异常上升、出现跨账号数据、Bootstrap 数据减少或离线状态误报。

- 脚本自动把 API 和 Web 同时恢复到记录的旧镜像。
- 本轮迁移保持加法兼容，不执行数据库降级。
- 如果错误涉及数据写入，停止写流量并从 P0 的备份副本分析；没有明确结论不得覆盖生产库。

### 完成标准

- [ ] GitHub CI 和 production environment 审批全部通过。
- [x] 生产 API/Web 均运行目标 SHA 镜像，公网 HTTPS 与 readiness 正常。
- [ ] 30 分钟观察窗口无异常，外部健康检查至少成功 3 次。
- [x] 已记录旧镜像、目标镜像、迁移编号和备份 ID；本次未触发回滚。

## P2：iPhone 真机与多端同步矩阵（未开始，受 P1 阻塞）

模拟器不能证明 WKWebView 在杀进程、系统清理存储、弱网和触觉反馈下的真实行为，因此必须用至少一台 iPhone；多端测试再增加一个 Web 或第二台 iPhone。

### 单机功能与持久化

1. 验证两种添加顺序：先输入名称再改优先级、先改优先级再输入名称；两者均只能生成一个任务。
2. 排序面板逐行拖动，确认触点与卡片一致；跨越一个槽位只产生一次轻触反馈，保存后重启仍保持顺序。
3. 在线创建任务后立即杀进程并重开；离线创建后杀进程、重开、恢复网络；任务和待同步操作都不得丢失或重复。
4. 退出账号后重新登录同一账号，再执行创建、完成、删除和排序；不得出现“操作失败”、旧请求写回或旧账号缓存覆盖。
5. 网络正常但服务端返回 401/409/429/500 时，界面必须显示对应认证、冲突、限流或服务异常，不能显示“离线待同步”。只有系统离线或真实传输失败才显示离线。
6. 使用系统存储压力场景后重开应用；如果 IndexedDB 被系统清理，应从云端恢复，并能使用“本地恢复数据”导出诊断文件。

### 多端与重复系列

1. A、B 两端同时编辑同一普通任务，确认明确进入冲突页，可选择云端或重新应用本地修改。
2. A 排序同时 B 创建/删除任务，最终两端顺序连续、任务不丢失。
3. A 修改重复系列“仅本次/本次及以后/整个系列”，B 使用旧版本再次修改；B 必须收到系列级冲突并能查看服务器系列与实例。
4. 完成的历史重复任务不得因未来计划调整而被删除或改期。
5. A→退出→B→退出→A 快速切换，旧代请求必须被取消，任何账号都不能看到另一账号数据。

### 完成标准

- [ ] 上述矩阵在真机 Release/TestFlight 包通过，并记录系统版本、App SHA 和测试证据。
- [ ] 连续 100 次前后台切换或 30 分钟弱网测试无丢失、重复、串号和虚假离线提示。
- [ ] 若真机上 IndexedDB 仍出现不可恢复丢失，再立项迁移原生 SQLite；验收通过前不提前引入第二套存储实现。

## P3：兼容窗口后的数据库收缩（尚不具备进入条件）

当前日期/时间字段采用“原字符串列 + typed shadow + 数据库触发器”的扩展阶段，以保证旧客户端和新后端可以共存。不要在首次发布时删除旧列。

### 进入条件

- P1、P2 均通过。
- 至少经历一个完整客户端强制更新/兼容观察窗口。
- 生产指标能确认活跃客户端均使用支持 typed 字段和权威 `TaskSeries` 的协议版本。
- 发布前备份已完成恢复演练。

### 收缩步骤

1. 运行只读一致性查询，确认每个 legacy/typed 字段的 NULL 状态和值完全一致，并保存计数报告。
2. 新版本后端改为只读 typed 字段；保留一版双写和一致性指标，不立即删除触发器。
3. 观察至少 7 天，无不一致后停止应用层 legacy 写入。
4. 新建独立 Prisma migration：删除 shadow 同步触发器和函数，将 typed 列重命名为稳定业务列，再更新索引和 Prisma schema。
5. 大表 DDL 在维护窗口执行，设置 lock timeout；拿不到锁就退出，不无限等待。
6. 再观察一个发布窗口后，才删除不再需要的旧协议分支和兼容测试。

### 完成标准

- [ ] 收缩前后行数、NULL 数和值一致性报告完全匹配。
- [ ] 迁移在生产数据副本完成计时和回滚演练。
- [ ] 生产无旧协议流量，typed-only 版本观察 7 天无异常。

## P4：无行为变化的结构清理（未开始，安排在发布稳定后）

这一阶段只降低维护成本，不改变布局、文案、颜色、动画曲线和业务语义；每次只移动一类代码，禁止与新功能混合提交。

### 方案

1. 把 `App.tsx` 中仍然独立的视图按边界拆到 `components/`：排序/任务详情、冲突处理、账号与安全、隐私和已删除任务。
2. 把本地缓存迁移、通知编排和任务显示格式化函数拆到各自模块；保持已经建立的 auth、sync、repository 状态机接口不变。
3. 删除前使用 `rg` 证明符号无引用；不做“看起来重复”的语义合并。
4. 每次机械拆分后执行 typecheck、完整单元/回归测试、PostgreSQL 集成、移动端 E2E 和 iOS 构建，并对关键页面截图比对。
5. 记录前后 bundle 大小、首屏时间、1,000 任务排序耗时；如果指标退化则撤销该次拆分。

### 完成标准

- [ ] `App.tsx` 只保留应用编排和页面路由，不再包含大型独立弹窗实现。
- [ ] 无未引用导出、重复状态源、调试日志和被绕过的存储/同步写路径。
- [ ] 自动测试和真机视觉/交互基线均无变化。

## 发布记录模板

每次生产操作在私有运维记录中填写：

```text
发布 SHA：e1257343703309fa83432fcb95cadd6183f74a6b
API 镜像：taskflow-api:e125734
Web 镜像：taskflow-web:e125734
旧 API/Web 镜像：taskflow-api:rollback-1724f41 / taskflow-web:rollback-1724f41
Prisma migration 数量与最后编号：25 / 20260914100000_series_schedule_exclusions
发布前备份 ID：/opt/taskflow-backups/preupdate-20260915-5a53497.dump（SHA256 与 pg_restore 列表校验通过）
OSS 下载恢复报告：未完成
开始/结束时间：2026-09-15（首次健康切换已完成，观察窗口待完成）
健康与业务抽查结果：API/Web/PostgreSQL healthy；公网 /ready 返回 ok，首页 200；2 个账户、2 个有效密码、137 条任务、25 个已完成迁移
异常与回滚结果：构建阶段发现 npm 重复安装和 Prisma 官方二进制站点超时，修复后重新构建成功；未触发生产回滚
操作人与复核人：Codex 执行，用户待完成真机业务验收
```
