# TaskFlow 剩余验收与优化计划

> 最后更新：2026-09-13
> 已完成的代码修复和清洗项已移除；这里只保留需要真实环境验证或属于后续架构演进的工作。

## 1. 当前状态

- 前端 TypeScript、Vite 生产构建、后端 TypeScript、45 项自动化测试、Capacitor iOS 同步和 Xcode iOS Simulator Debug 构建均已通过。
- 最新构建已安装并启动到 iPhone 17（iOS 26.5）模拟器，启动页和登录页渲染正常。
- 任务创建提交已统一从真实表单快照读取，覆盖“先输入标题再改优先级”和“先改优先级再输入标题”。
- 排序反馈已经具备 start/change/end 生命周期，并按实际跨槽位去重。
- 任务动作增加动画中断兜底提交，不再依赖单个 `onExitComplete` 回调。
- 退出、刷新令牌、重登、本地持久化和原生冷启动恢复的竞态已经收敛。
- 后端任务版本更新采用原子 CAS；排序、任务写入和同步序号使用一致的用户级锁顺序；顺序集合变化返回可恢复冲突及云端权威顺序。
- 已删除 TypeScript 审计确认的 6 个零引用声明，以及生产不可达的旧同步合并和 single-flight 实现与测试。
- 已删除入口不可达的 46 个 UI 模板文件及 34 个直接依赖（共减少 101 个安装包）；生产 CSS 从约 105 KB 降至 44 KB，完整检查未发现行为回归。

## 2. 必须完成的人工验收

### 2.1 iOS 模拟器

- [ ] 解锁 Mac 后，在已安装的最新构建中登录测试账号。
- [ ] 快速创建与完整详情界面分别验证：标题→P1/P2/P3、P1/P2/P3→标题，中文和英文各一次。
- [ ] 检查创建结果的标题、优先级、日期与同步后的服务端数据完全一致，没有重复任务。
- [ ] 拖动第 1 个任务到中间和末尾，确认触点、卡片和落点一致；再验证上下移动按钮。
- [ ] 在 Flow 动画开始后立即切换页面，确认完成/跳过/稍后仍只提交一次。
- [ ] 退出后立刻用同一账号登录，再做 A→B→A 账号切换，确认缓存和待同步队列不串号。

当前自动化界面操作的唯一阻塞是 Mac 处于锁屏状态；代码构建与安装本身没有阻塞。

### 2.2 iPhone 真机

- [ ] 使用中文拼音输入法执行两种任务创建顺序，第一次提交即成功。
- [ ] 拖动跨过 1、2、3 个槽位，确认每个有效槽位一次轻触觉，同槽位不连续振动，放手/关闭后无延迟振动。
- [ ] 记录 10、100、500 个任务下的点击响应、主线程 Long Task、FPS 和掉帧比例。
- [ ] 开启“减少动态效果”后复测任务动作和排序，最终状态必须一致。

## 3. 后端与多设备集成验收

- [ ] 在独立测试 PostgreSQL 中并发发送相同 `baseVersion` 的两个更新，确认仅一个 CAS 成功，另一个返回 `TASK_CONFLICT`。
- [ ] 两个 deviceId 并发执行创建/完成/删除与排序，确认无死锁、无 500、无任务遗漏，冲突可选择云端或重放本地顺序。
- [ ] 验证断网启动→创建/编辑→杀进程→恢复网络，`pending_operations` 和 `sync_meta` 能恢复并自动上传。
- [ ] 验证 refresh 的 401、404/426、429、5xx、超时和真实断网分别显示正确状态，临时失败不会永久废弃待同步操作。
- [ ] 部署前备份生产数据库；部署后检查 migration、`/api/v1/health`、登录、同步和退出；保留旧镜像与数据库回滚点。

## 4. 后续架构优化（不与本轮修复混合发布）

### 4.1 可测试性与模块边界

- [ ] 将 `App.tsx` 按 Task、Sync、Notification、Stats 和页面组件机械拆分；第一阶段只搬移，不改变 JSX、样式或状态语义。
- [ ] 用 React 组件测试和移动端 Playwright 测试替代关键业务路径的源码正则断言；正则测试只保留为结构护栏。
- [ ] 建立 CI：严格未使用审计、typecheck、前后端 build、单元测试、PostgreSQL 集成测试、Web E2E 和 iOS 冒烟。

### 4.2 数据模型与契约

- [ ] 使用共享 Zod schema 或 OpenAPI 生成 Task、SyncOperation、SyncResponse、AuthUser 和 UserStats 类型。
- [ ] 为统计响应增加 `version` 或 `computedAt`，覆盖双设备完成、跨午夜、时区改变和删除恢复。
- [ ] 将重复任务演进为权威 TaskSeries，明确“仅本次 / 本次及以后 / 整个系列”，未来实例按需生成。
- [ ] 增加密码重置、密码修改、设备/会话列表、全部设备退出和敏感操作近期重新认证。

### 4.3 持续清洗护栏

- [ ] CI 固定运行 `noUnusedLocals`/`noUnusedParameters`，防止零引用代码重新进入主线。
- [ ] 每批清洗前后保存关键页面截图和可访问树快照，并运行完整检查；不在清洗批次升级依赖或调整动画参数。
- [ ] 保持存储 key、API schema、文案、布局、颜色、尺寸和动画结果不变。

## 5. 完成标准

- 自动化：`npm run check`、严格未使用审计、PostgreSQL 并发测试、Web E2E、Capacitor 同步和 iOS 构建全部通过。
- 交互：模拟器和真机完成任务创建、排序、动作动画、退出重登及离线恢复矩阵。
- 数据：不删除或覆盖现有本地任务、待同步队列和云端数据；多账号隔离、幂等、冲突和游标行为正确。
- 发布：生产数据库有可验证备份，后端和客户端均有明确回滚版本，发布后冒烟通过。
